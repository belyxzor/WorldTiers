import React from 'react';import overallIcon from '../../assets/modes/overall.png';import {MODES} from '../../config/modes';import {ModeIcon} from '../common/ModeIcon';
export function ModeNavigation({active='overall',navigate}){return <nav className="mode-nav"><button className={active==='overall'?'active':''} onClick={()=>navigate('/home')}><img className="overall-mode-icon" src={overallIcon} alt=""/>Overall</button>{MODES.map(m=><button key={m.id} className={active===m.id?'active':''} onClick={()=>navigate(`/mode/${m.id}`)}><ModeIcon mode={m} size="sm"/>{m.name}</button>)}</nav>}
