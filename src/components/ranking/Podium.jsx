import React from 'react';import {avatarUrl,handleSkinError} from '../../services/skinService';
export function Podium({players,navigate}){return <section className="leader-highlights" aria-label="Joueurs à la une"><header><div><small>À LA UNE</small><h2>Les leaders de la saison</h2></div><p>Les trois meilleurs scores actuellement validés.</p></header><div className="leader-grid">{players.slice(0,3).map((player,index)=><button key={player.id} className={`leader-card rank-${index+1}`} onClick={()=>navigate(`/player/${player.username}`)}><span className="leader-index">{String(index+1).padStart(2,'0')}</span><img src={avatarUrl(player,96)} onError={e=>handleSkinError(e,player,96)} alt=""/><span className="leader-copy"><small>Rang actuel</small><b>{player.username}</b><em>{player.points} <i>points</i></em></span><span className="leader-arrow">↗</span></button>)}</div></section>}
