import React from 'react';

export function NotFoundPage({navigate,onSecret,loginOpen,password,onPasswordChange,onLogin,loginError}){return <main className="not-found"><article className="not-found-card"><div className="not-found-orbit"><span>W</span></div><small>ERREUR DE NAVIGATION</small><strong>404</strong><h1>Cette route n’existe pas.</h1><p>La page demandée a peut-être été déplacée, supprimée ou son adresse est incorrecte.</p><div className="not-found-actions"><button className="primary" onClick={()=>navigate('/home')}>Retour au classement</button><button onClick={()=>navigate('/infos')}>Voir le guide</button></div><div className="not-found-links"><button onClick={()=>navigate('/stats')}>Statistiques</button><span>•</span><a href="/api">Documentation API ↗</a></div></article>{onSecret&&<button className="not-found-secret" aria-label="Accès privé" onClick={onSecret}/>} {loginOpen&&<div className="private-login"><form onSubmit={onLogin}><small>ACCÈS PRIVÉ</small><h2>Mot de passe</h2><input type="password" autoFocus value={password} onChange={event=>onPasswordChange(event.target.value)} placeholder="••••••••" required/><button className="primary">Ouvrir</button>{loginError&&<p>{loginError}</p>}</form></div>}</main>}
